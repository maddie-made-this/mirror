import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Origins permitted to load the dev server's internal resources (/_next chunks,
  // HMR, server actions). Localhost is allowed by default, so nothing is listed
  // here. Add a hostname only if you serve the dev server through a tunnel.
  /* config options here */
};

export default nextConfig;
